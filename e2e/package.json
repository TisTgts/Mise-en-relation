{
  "name": "mise-en-relation-e2e",
  "version": "1.0.0",
  "private": true,
  "description": "Tests end-to-end Playwright pour la plateforme de mise en relation (isolés du code applicatif).",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui",
    "test:public": "playwright test public auth-flow access-control",
    "test:client": "playwright test client",
    "test:fournisseur": "playwright test fournisseur",
    "test:admin": "playwright test admin",
    "test:workflow": "playwright test workflow",
    "report": "playwright show-report",
    "install:browser": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0"
  }
}
