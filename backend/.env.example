# Copier vers backend/.env — NE PAS committer backend/.env
# Branche deploy/toghinis-com — voir deploy/ovh/DEPLOY_TOGHINIS_COM.md

SECRET_KEY=generer-avec-python-secrets-token_urlsafe
DEBUG=False
ALLOWED_HOSTS=toghinis.com,www.toghinis.com,144.217.82.132

# Base dédiée (distincte de toghinis.net)
DATABASE_URL=postgres://tis_com:MOT_DE_PASSE@localhost:5432/plateforme_com_db

CORS_ALLOWED_ORIGINS=https://toghinis.com,https://www.toghinis.com
CORS_ALLOW_ALL_ORIGINS=False

JWT_ACCESS_TOKEN_LIFETIME=60
JWT_REFRESH_TOKEN_LIFETIME=1440

THROTTLE_LOGIN=5/min
THROTTLE_REGISTER=3/min
THROTTLE_TOKEN_REFRESH=10/min
THROTTLE_ANON=100/hour
THROTTLE_USER=1000/hour

REDIS_URL=redis://localhost:6379/1
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# Pays actif (prioritaire sur pays/active.json) — tg | bf
COUNTRY_CODE=tg
